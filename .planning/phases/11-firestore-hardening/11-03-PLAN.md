---
phase: 11-firestore-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified: []
autonomous: false
requirements:
  - ADR-0001-SEC
  - ADR-0001-PERF
  - ADR-0001-AGG
  - ADR-0001-BACKFILL

must_haves:
  truths:
    - "All Phase 11 changes verified working in the browser"
    - "Security rules deployed to Firebase and tested"
    - "Dashboard reads from date-sharded summaries correctly"
---

<objective>
Human verification of the complete Phase 11 Firestore hardening implementation.

Purpose: Confirm security rules work correctly, dashboard reads from optimized paths,
billing writes are atomic, and backfill script functions for existing data.

Output: Phase 11 UAT sign-off and 11-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-firestore-hardening/11-01-SUMMARY.md
@.planning/phases/11-firestore-hardening/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 11 — Firestore Architecture Hardening:
    1. **Security Rules v2** — Role-based access (owner/admin/cashier), write validation, immutable sales
    2. **Date-Sharded Aggregation** — billing writes daily_summary + monthly_summary atomically
    3. **Optimized Dashboard** — reads 8 docs instead of scanning hundreds of sales
    4. **Owner Bootstrap** — auth.js sets staff_roles.{email}='owner' on first sign-up
    5. **Backfill Script** — one-time migration for existing sales data
    6. **Legacy Removal** — summary/totals no longer written by billing
  </what-built>
  <how-to-verify>
    **Step 1: Deploy Security Rules**
    - Open Firebase Console → Firestore → Rules tab
    - Copy-paste the content of `firestore.rules` from the repo
    - Click "Publish"

    **Step 2: Test Billing Still Works**
    - Navigate to #/billing
    - Create a test sale (add item, submit)
    ✓ Sale succeeds without errors
    - Open Firestore Console → shops/{SHOP_ID}/daily_summary/
    ✓ A document for today's date exists with count=1, revenue matching the sale total
    - Check shops/{SHOP_ID}/monthly_summary/{YYYY-MM}
    ✓ Document exists with count=1, revenue matching

    **Step 3: Test Dashboard Optimization**
    - Navigate to #/dashboard
    ✓ Today's revenue matches the test sale
    ✓ Week and month stats populate correctly
    ✓ Bar chart shows today's bar

    **Step 4: Test Security (Optional but Recommended)**
    - If you have a second staff email (cashier role):
      - Sign in as cashier
      - Try to edit inventory item name/price → should work only for stock changes
      - Try to access Settings → staff management → add/remove should fail
    - Or test via Firebase Console Rules Playground:
      - Simulate a write to /shops/shop_001/config/main with a non-owner email → should DENY

    **Step 5: Run Backfill (if existing sales data)**
    - Open the app in Chrome, sign in as owner
    - Open DevTools Console (F12)
    - Paste the content of `scripts/backfill-summaries.js` and press Enter
    ✓ Console shows "Processed X sales → Y daily, Z monthly docs"
    ✓ Console shows "✓ Backfill complete!"
    - Refresh dashboard → historical stats should now appear in bar chart

    **Step 6: Verify Immutable Sales**
    - In Firebase Console Rules Playground:
      - Simulate an UPDATE to /shops/shop_001/sales/{any_sale_id} → should DENY
      - Simulate a DELETE → should DENY
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 11 complete, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All Phase 11 items confirmed:
- [ ] Security rules deployed and enforcing role-based access
- [ ] Billing batch writes daily_summary + monthly_summary atomically
- [ ] Dashboard reads from date-sharded docs (not sales collection)
- [ ] Backfill script works for existing data
- [ ] Sales are immutable (update/delete denied)
- [ ] Owner role set on first bootstrap
</verification>

<success_criteria>
User types "approved" — Phase 11 complete.
STATE.md updated to Phase 11 complete.
ADR-0001 status changed from "Proposed" to "Accepted".
</success_criteria>

<output>
After completion, create `.planning/phases/11-firestore-hardening/11-03-SUMMARY.md`

Update STATE.md and ADR-0001 status.
</output>
