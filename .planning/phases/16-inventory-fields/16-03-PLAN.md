---
phase: 16-inventory-fields
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01-PLAN.md
  - 16-02-PLAN.md
files_modified: []
autonomous: false
requirements:
  - FR-06

must_haves:
  truths:
    - "All UAT items for Phase 16 verified by the owner"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 16 deliverables — inventory form fields, list display, filters, billing grid, and export.

Purpose: Confirm the complete inventory fields enhancement works end-to-end in the live app.
Output: Owner sign-off on Phase 16 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- modules/inventory.js: Type, Branch, Color fields added to Add/Edit item modal; inventory table has 7 columns; filter dropdowns (Type, Branch) above the list; mobile cards show color badge + type/branch meta
- modules/billing.js: Color badge on product cards; Type and Branch filter dropdowns above the product grid
- modules/export.js: Inventory Excel export includes Type, Branch, Color columns
  </what-built>
  <how-to-verify>
Open the app and verify each of the following:

**Inventory screen:**
1. Tap "Add Item" → confirm Type, Branch, Color text fields appear in the form
2. Add a test item: Type="Doors", Branch="Main", Color="White" → save
3. Inventory list: confirm Type and Branch columns appear in the table (desktop) or meta text in mobile card
4. Color "White" shows as a badge on the mobile card
5. Filter by Type "Doors" → only matching items shown; reset to "All Types" → full list returns
6. Filter by Branch "Main" → only matching items shown
7. Tap "Edit" on the test item → Type/Branch/Color fields are pre-populated correctly → save a change → verify it persists

**Billing screen:**
8. Navigate to billing → two filter dropdowns appear above the product grid
9. Select "Doors" in the Type filter → grid shows only doors items
10. The test item's card shows "White" as a color badge

**Export:**
11. Dashboard → Export → Inventory → download .xlsx
12. Open the file → confirm columns: ID, Name, Type, Branch, Color, Unit, Price, Stock, Threshold, Status
13. Test item row has correct Type, Branch, Color values
14. Existing items (without new fields) show blank cells in Type/Branch/Color columns — no crash

**Edge cases:**
15. Existing items (no type/branch/color) still display and edit correctly
16. No JS console errors throughout
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None — this plan is a human verification checkpoint with no code changes.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | N/A | Verification checkpoint | accept | No code written in this plan |
</threat_model>

<verification>
Owner approval received via resume-signal "approved".
</verification>

<success_criteria>
All 16 UAT checklist items pass. Owner types "approved".
</success_criteria>

<output>
After completion, create `.planning/phases/16-inventory-fields/16-03-SUMMARY.md`
</output>
