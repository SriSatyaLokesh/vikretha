---
phase: 03-billing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/dashboard.js
autonomous: true
requirements:
  - FR-02.1
  - FR-04.1

must_haves:
  truths:
    - "'New Sale' CTA card is the most prominent element on the dashboard"
    - "Tapping 'New Sale' navigates to #/billing"
    - "Dashboard shows 3 summary placeholder cards (Today, This Week, This Month)"
    - "Dashboard renders without JS errors when no summary data exists in Firestore"
  artifacts:
    - path: "modules/dashboard.js"
      provides: "Dashboard stub — New Sale CTA + summary card placeholders"
      exports: ["render"]
      min_lines: 50
  key_links:
    - from: "modules/dashboard.js"
      to: "#/billing"
      via: "window.location.hash = '#/billing' on New Sale button click"
---

<objective>
Create `modules/dashboard.js` — a minimal but functional dashboard stub that satisfies FR-02.1 ("New Sale" button prominent on dashboard) and provides placeholder cards for Phase 5's reporting features.

Purpose: Users land on the dashboard after sign-in. They need an obvious entry point to start recording a sale. Phase 5 will fill the summary cards with live data from Firestore.

Output: A functional `modules/dashboard.js` with a prominent "New Sale" call-to-action and three summary placeholder cards (Today, This Week, This Month). No Firestore reads in this phase — static placeholder values shown with a note to indicate live data comes in Phase 5.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@modules/auth.js
@shop.config.js
@styles/main.css

<interfaces>
<!-- Module pattern: export function render(container), use innerHTML, attach listeners after -->

CSS classes available: .card, .btn, .btn-primary, .btn-full, .empty-state
CSS variables: --theme-color, --bg-primary, --bg-surface, --text-primary, --text-secondary, --border, --success

app.js route handler calls: `import('./modules/dashboard.js').then(m => m.render(content))`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: modules/dashboard.js — New Sale CTA + summary placeholders</name>
  <files>modules/dashboard.js</files>
  <action>
Create `modules/dashboard.js`. Export a single `render(container)` function.

**Layout structure (set via container.innerHTML):**

```html
<div id="dashboard-screen" style="display:flex;flex-direction:column;gap:16px;">

  <!-- New Sale CTA — most prominent element -->
  <button id="new-sale-btn" class="btn btn-primary btn-full"
    style="padding:18px;font-size:1.1rem;font-weight:700;
           box-shadow:0 4px 12px rgba(37,99,235,0.35);border-radius:12px;">
    + New Sale
  </button>

  <!-- Summary section heading -->
  <div style="display:flex;justify-content:space-between;align-items:center;">
    <h2 style="font-size:0.875rem;font-weight:600;color:var(--text-secondary);
               text-transform:uppercase;letter-spacing:0.05em;">
      Summary
    </h2>
    <span style="font-size:0.75rem;color:var(--text-muted);">Live data in Phase 5</span>
  </div>

  <!-- Summary cards (3 × placeholder) -->
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">

    <!-- Today card (full width) -->
    <div class="card" style="grid-column:1/-1;">
      <p style="font-size:0.75rem;color:var(--text-secondary);margin-bottom:4px;">Today</p>
      <p style="font-size:1.5rem;font-weight:700;color:var(--text-primary);">—</p>
      <p style="font-size:0.8rem;color:var(--text-muted);">0 sales · ₹0 revenue</p>
    </div>

    <!-- This Week card -->
    <div class="card">
      <p style="font-size:0.75rem;color:var(--text-secondary);margin-bottom:4px;">This Week</p>
      <p style="font-size:1.25rem;font-weight:700;color:var(--text-primary);">—</p>
      <p style="font-size:0.75rem;color:var(--text-muted);">0 sales</p>
    </div>

    <!-- This Month card -->
    <div class="card">
      <p style="font-size:0.75rem;color:var(--text-secondary);margin-bottom:4px;">This Month</p>
      <p style="font-size:1.25rem;font-weight:700;color:var(--text-primary);">—</p>
      <p style="font-size:0.75rem;color:var(--text-muted);">0 sales</p>
    </div>

  </div>
</div>
```

**Event listener (attach after innerHTML):**
```javascript
container.querySelector('#new-sale-btn').addEventListener('click', () => {
  window.location.hash = '#/billing';
});
```

No Firestore imports needed in this plan — summary data is Phase 5.

**No escapeHtml needed** — all strings are static literals (no user data rendered).
  </action>
  <verify>
    Navigate to `#/dashboard` after sign-in. "New Sale" button visible and prominent. Clicking it navigates to `#/billing`. Three summary cards visible with placeholder "—" values.
  </verify>
  <done>
    Dashboard renders with "New Sale" CTA and 3 summary placeholders. No JS errors. "New Sale" button navigates to billing screen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Dashboard stub has no Firestore reads/writes. All content is static. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03d-01 | T (Tampering) | Route navigation via hash change | accept | Hash routing is client-side only; no server-side data is affected by hash manipulation. Route guard in app.js enforces authentication. |
</threat_model>

<verification>
1. `modules/dashboard.js` exports `render` function
2. Dashboard loads at `#/dashboard` without JS errors
3. "New Sale" button visible as most prominent UI element
4. Clicking "New Sale" navigates to `#/billing`
5. Three summary cards present with placeholder values
</verification>

<success_criteria>
- `modules/dashboard.js` exists and exports `render(container)`
- Dashboard renders without errors when no Firestore data exists
- "New Sale" button navigates to `#/billing`
- No broken layouts on mobile (375px width)
</success_criteria>

<output>
After completion, create `.planning/phases/03-billing/03-02-SUMMARY.md` describing what was built.
</output>
