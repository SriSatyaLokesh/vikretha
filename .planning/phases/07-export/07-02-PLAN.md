---
phase: 07-export
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - modules/dashboard.js
autonomous: false
requirements: [FR-06.8]

must_haves:
  truths:
    - "Export button is visible on the dashboard screen"
    - "Clicking Export reveals a dropdown with 3 options"
    - "Sales (month) exports .xlsx for currently-selected month in the picker"
    - "Sales (all) exports .xlsx for all historical sales"
    - "Inventory export downloads .xlsx with all inventory items"
    - "Files download correctly on mobile (Chrome Android)"
    - "Export works offline using Firestore cached data"
  artifacts:
    - path: "modules/dashboard.js"
      provides: "Export anchor div in HTML template + attachExportMenu import + call in render()"
      contains: "dash-export-anchor"
  key_links:
    - from: "modules/dashboard.js"
      to: "modules/export.js"
      via: "import { attachExportMenu } from './export.js'"
      pattern: "attachExportMenu"
    - from: "attachExportMenu(container)"
      to: "#dash-export-anchor"
      via: "container.querySelector"
      pattern: "dash-export-anchor"
---

<objective>
Wire the Export menu into the dashboard and verify all exports work end-to-end.

Purpose: Makes the export feature discoverable and usable — Export button appears below the monthly report on the dashboard, wired to all three export functions.

Output: dashboard.js modified (import + anchor div + attachExportMenu call), then human verification of all export paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@modules/dashboard.js
@modules/export.js
@.planning/phases/07-export/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 output -->

From modules/export.js:
```javascript
export function attachExportMenu(container);
// Finds #dash-export-anchor inside container, injects Export button + dropdown.
// Must be called after dashboard HTML is in the DOM.
```

In dashboard.js render(), the container HTML must include:
```html
<div id="dash-export-anchor" style="padding:0 0 8px;"></div>
```
Placed after the monthly-report-card div and before the closing `</div>` of the scrollable content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Export menu into dashboard.js</name>
  <files>modules/dashboard.js</files>
  <action>
Make two changes to modules/dashboard.js:

**Change 1 — Add import at the top of the file** (after existing imports):
```javascript
import { attachExportMenu } from './export.js';
```

**Change 2 — Add anchor div to the HTML template** inside the `render()` function.
Find the closing `</div>` that ends the main scrollable content div (the one wrapping all dashboard cards — after the `#monthly-report-card` div).
Insert `<div id="dash-export-anchor" style="padding:0 0 8px;"></div>` immediately after the closing `</div>` of `#monthly-report-card` and before the outer container's closing tag.

The section in dashboard HTML looks like:
```html
      <!-- Monthly Report -->
      <div id="monthly-report-card" class="card monthly-report">
        ...
      </div>

    </div>`;   ← outer scroll div closes here
```

Insert the anchor between them:
```html
      </div>   ← close #monthly-report-card

      <!-- Export -->
      <div id="dash-export-anchor" style="padding:0 0 8px;"></div>

    </div>`;   ← outer scroll div closes here
```

**Change 3 — Call attachExportMenu** at the end of `render()`, after all other event listeners are wired (after the `_unsubSummary = onSnapshot(...)` block):
```javascript
  // Wire export menu (Phase 7)
  attachExportMenu(container);
```

Do NOT restructure any other part of dashboard.js. Only add the import line, the anchor div, and the single function call.
  </action>
  <verify>
    <automated>Select-String -Path "modules/dashboard.js" -Pattern "attachExportMenu|dash-export-anchor" | Select-Object Line, LineNumber</automated>
  </verify>
  <done>
    dashboard.js contains `import { attachExportMenu } from './export.js'`, contains `id="dash-export-anchor"` in HTML template, and calls `attachExportMenu(container)` at end of render(). No other lines changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Data Export feature:
    - modules/export.js with SheetJS lazy-loading and 3 export functions
    - Export button + dropdown on the dashboard (below monthly report)
    - Sales (month) / Sales (all) / Inventory .xlsx downloads
    - Progress overlay for large datasets
  </what-built>
  <how-to-verify>
    1. Open the app in browser: http://localhost:8080 (or the GitHub Pages URL)
    2. Sign in and navigate to Dashboard (#/dashboard)
    3. **Verify Export button visible:** Scroll to bottom of dashboard — you should see an "Export" button below the Monthly Report card
    4. **Test dropdown:** Click "Export" → dropdown should appear with 3 options: "Sales (This Month)", "Sales (All Time)", "Inventory"
    5. **Test Sales (This Month):**
       - Select a month in the Monthly Report picker (e.g., current month)
       - Click Export → "Sales (This Month)"
       - Progress overlay should appear briefly, then a .xlsx file should download
       - Open the file — verify columns: Sale ID, Date, Items, Qty, Subtotal, Discount, Total, Phone
       - Verify filename: `{ShopName}_sales_{YYYY-MM}.xlsx`
    6. **Test Inventory export:**
       - Click Export → "Inventory"
       - .xlsx file downloads, verify columns: ID, Name, Unit, Price, Stock, Threshold, Status
       - Low-stock items should show "Low Stock" in Status column
       - Filename: `{ShopName}_inventory_{YYYY-MM-DD}.xlsx`
    7. **Test Sales (All Time):** Click Export → "Sales (All Time)" → verify .xlsx downloads with all sales
    8. **Test offline:** Enable "Offline" in DevTools Network tab → click Export → verify it still downloads from Firestore cache (no error)
    9. **Close dropdown test:** Click Export → click anywhere outside dropdown → dropdown should close
  </how-to-verify>
  <resume-signal>Type "approved" if all exports work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard.js → export.js | Module boundary; export.js called with container reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | attachExportMenu call | accept | Container passed by reference from auth-gated dashboard; no untrusted input at this boundary |
| T-07-06 | Elevation of Privilege | Export access | accept | Export only available post-authentication (dashboard is behind auth guard in app.js); Firestore security rules enforce shop isolation |
</threat_model>

<verification>
After Task 1:
- `Select-String -Path "modules/dashboard.js" -Pattern "attachExportMenu|dash-export-anchor"` returns 3 matches

After checkpoint approval:
- All 3 export types produce downloadable .xlsx files
- Files open correctly in Excel/Sheets/LibreOffice
- Offline export works via Firestore cache
</verification>

<success_criteria>
- Export button appears on dashboard below Monthly Report
- All three exports produce valid .xlsx files with correct columns and filenames
- Progress overlay shows/hides correctly
- Dropdown closes on outside click
- Offline export does not error
- Human verified and approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-export/07-02-SUMMARY.md`
</output>
