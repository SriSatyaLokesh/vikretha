---
phase: 18-animation-toast
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01-PLAN.md
  - 18-02-PLAN.md
files_modified: []
autonomous: false
requirements:
  - FR-09.1
  - FR-09.2
  - FR-09.3
  - FR-09.4
  - FR-09.5
  - FR-09.6
  - FR-09.7
  - NFR-13
  - NFR-17

must_haves:
  truths:
    - "All Phase 18 UAT items verified by the owner in the live app"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 18 deliverables — toast notifications, page transitions, cart animations, and reduced-motion accessibility.

Purpose: Confirm the complete Animation & Toast Notification System works end-to-end in the live app before advancing to Phase 19.
Output: Owner sign-off on Phase 18 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- `lib/toast.js` — toast manager: `toast.success()`, `toast.error()`, `toast.warn()`, `toast.info()`, `toast.confirm()`; max 5 stack; auto-dismiss 3s; slide-in/slide-out CSS animation
- `index.html` — `#toast-container` added before `</body>`
- `styles/main.css` — toast CSS (sections 20–24): toast container, toast variants, enter/exit keyframes, `.page-enter`, `.cart-item-enter`, `.cart-item-exit`, `.sheet-enter`, `prefers-reduced-motion` block
- `modules/settings.js` — `confirm()` replaced with `toast.confirm()` callback; `alert()` replaced with `toast.error()`
- `modules/export.js` — all 3 `alert('Export failed...')` replaced with `toast.error()`
- `modules/billing.js` — `toast.success('Sale recorded ✓')` on submit success; `toast.error()` on failure; `.cart-item-enter` on all cart rows
- `app.js` — `page-enter` class added to `#app-content` on every route change
  </what-built>
  <uat-checklist>
- [ ] Submit a sale → green toast "Sale recorded ✓" appears bottom-right, auto-dismisses in 3s
- [ ] Submit with empty cart → toast.warn fires (in addition to inline error message)
- [ ] Trigger export error (e.g. disconnect network) → red toast "Export failed: ..."
- [ ] Settings → remove a staff member → toast confirm dialog appears with Confirm/Cancel buttons
- [ ] Clicking Confirm on staff removal → staff removed; Clicking Cancel → nothing happens
- [ ] Navigate between screens → content slides in (page-enter animation)
- [ ] Add item to cart → row slides in from left (cart-item-enter animation)
- [ ] All previous functionality still works: billing, inventory, reports, settings, export
  </uat-checklist>
  <instructions>
Open the live app at your GitHub Pages URL (or local server). Work through each UAT item above.
When all items are verified, reply "Phase 18 approved" to advance to Phase 19.
  </instructions>
</task>

</tasks>
