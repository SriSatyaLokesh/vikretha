---
phase: 15-sales-history-mgmt
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified: []
autonomous: false
requirements:
  - FR-04.8
  - FR-04.9
  - FR-04.10
---

<objective>
Human verification checkpoint for Phase 15 — Sales History & Bill Management.

Purpose: Confirm all three feature areas work correctly end-to-end on a real device/browser before marking the phase complete.

Output: Phase 15 marked complete, STATE.md updated, summaries committed.
</objective>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 15 — Sales History & Bill Management:
    1. Sales history list at #/reports — paginated, date-filtered, searchable
    2. Sale detail panel — full data, resend receipt navigation
    3. Owner-only bill editing — inline edit form, Firestore updateDoc, audit trail
    4. Firestore rules amendment — owner can update sales, cashier cannot
  </what-built>
  <how-to-verify>
    **Sales history list:**
    1. Navigate to #/reports — list of past sales renders (not "coming soon")
    2. Each row shows date, Sale ID, customer name (or —), and total amount
    3. Set a date range (From / To) and click Filter — list narrows to that range
    4. Type a customer name or Sale ID in the search box — rows filter live
    5. If you have >25 sales, click "Load more" — next batch appends

    **Sale detail + Resend Receipt:**
    6. Click any sale row — detail panel opens (slides up on mobile, centered on desktop)
    7. Detail shows: Sale ID, date, all line items with qty/price/size, subtotal, discount, total
    8. If customer_name was recorded, it appears in the panel
    9. Click "View / Resend Receipt" — navigates to #/receipt/{saleId} and receipt canvas renders
    10. WhatsApp share button on the receipt page works

    **Owner bill editing:**
    11. Signed in as owner: "✏️ Edit Bill" button appears in the detail panel
    12. Click Edit Bill — form expands showing editable item rows
    13. Change a qty or price — totals update live
    14. Click "Save changes" — success (no error), detail re-renders with amended data
    15. Amended detail shows "⚠️ Amended by {email} on {date}" notice and original total
    16. ✏️ badge appears on the row in the sales list for the amended sale

    **Cashier role:**
    17. Sign in as a cashier account (if available) — "Edit Bill" button is NOT visible
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<output>
After approval:
1. Update STATE.md: Phase 15 status → ✅ Complete
2. Update ROADMAP.md Phase 15 Plans section: mark all plan checkboxes [x]
3. Commit: `git add . && git commit -m "feat(15-sales-history-mgmt): phase complete"`
</output>
