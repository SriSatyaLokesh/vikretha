---
phase: 21-branded-receipt
plan: "02"
type: execute
wave: 2
depends_on: [21-01]
files_modified: []
autonomous: false
requirements: [FR-12.1, FR-12.2, FR-12.3, FR-12.4]

must_haves:
  truths:
    - "Shop owner verified receipt branding works end-to-end in live app"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 21 — Branded Receipt.

Purpose: Shop owner confirms the Settings branding form works and receipt canvas correctly displays the configured logo and footer text.
Output: Phase 21 approved and complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 21: Branded Receipt

    - Settings screen has a new "Receipt Branding" card with:
      - Logo URL input (pre-fills from Firestore if previously saved)
      - Footer text textarea (pre-fills from Firestore if previously saved)
      - "Save Branding" button → saves to Firestore config/main
    - Receipt canvas now reads logo and footer dynamically from Firestore at render time
    - Logo loads with a 2-second timeout — receipt always renders even if image is slow/broken
    - Footer falls back to "THANK YOU FOR SHOPPING!" if not configured
  </what-built>
  <how-to-verify>
    1. Open the app → navigate to **Settings**
    2. Confirm the **Receipt Branding** card is visible with Logo URL input, Footer text textarea, and "Save Branding" button
    3. Enter a Logo URL (use any public image URL, e.g. your shop's logo or a placeholder) and a custom footer message (e.g. "Thank you! Visit us again.")
    4. Click **Save Branding** → confirm toast "Receipt branding saved" appears
    5. Navigate to any sale → **View Receipt**
    6. Confirm the logo appears at the **top** of the receipt canvas (centered, ~56×56px)
    7. Confirm the custom footer text appears in the **footer section** of the receipt (where "THANK YOU FOR SHOPPING!" used to be)
    8. Download the receipt PNG → confirm logo and footer are present in the downloaded image
    9. Go back to Settings → confirm Logo URL and Footer text are still pre-filled (persisted correctly)
    10. **Edge case — empty logo URL:** Clear the Logo URL field and save → open a receipt → receipt renders normally without a logo (no crash, no blank receipt)
    11. **Edge case — broken logo URL:** Enter a URL that returns 404 (e.g. `https://example.com/notfound.png`) and save → open a receipt → receipt still renders after a short delay (2s timeout), just without the logo image
    12. **Edge case — no footer:** Clear the Footer text field and save → open a receipt → footer shows "THANK YOU FOR SHOPPING!" fallback text
    13. Test on **mobile** (≤ 430px) — receipt preview layout looks correct, no overflow
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 21, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
UAT criteria from ROADMAP Phase 21:
- [ ] Settings → Receipt Branding section shows Logo URL + Footer text inputs
- [ ] Save branding → receipt immediately shows logo + footer on next bill
- [ ] Logo renders at correct size/position on receipt canvas
- [ ] If logo URL is empty or fails to load → receipt renders without logo (no crash, no blank receipt)
- [ ] Footer text appears at bottom of receipt canvas
- [ ] Downloaded PNG includes logo + footer
</verification>

<success_criteria>
All 6 UAT criteria verified by shop owner. Phase 21 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/21-branded-receipt/21-02-SUMMARY.md`
</output>
