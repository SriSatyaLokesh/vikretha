---
phase: 13-inventory-piece-sizes
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified: []
autonomous: false
requirements:
  - FR-05.1
  - FR-05.2
  - FR-02.1
  - FR-03.1
---

<objective>
Human verification of Phase 13 — Inventory Item Sizes & Piece Variant Quantities.

All automated work (Plans 01 and 02) must be complete before this checkpoint.
</objective>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plans 01 and 02 implemented:
- Inventory add/edit: "Pieces" default unit; "Other" reveals custom unit text
- Piece items: "Has sizes?" toggle + dynamic size variant rows (label, width, PSI, stock per variant)
- Firestore schema: unitType, hasSizes, sizes map on inventory docs
- isLow: checks per-size stock when hasSizes=true
- Inventory list: size count badge + summed stock for sized items
- Billing: size picker sheet when tapping a product with size variants
- Cart row shows item name + size label
- Sale submit: decrements per-size stock via dot-notation field path
- Receipt canvas: size label sub-line on each sized line item
  </what-built>
  <how-to-verify>
Run app at http://localhost:8080 (or GitHub Pages URL).

**Inventory — Add flow:**
1. Go to Inventory tab → tap "+" FAB
2. Confirm "Pieces" is selected by default in the unit toggle
3. Tap "Other" → verify a custom unit text input appears; type "kg"
4. Tap "Pieces" back → verify custom unit input hides
5. Check "Has sizes?" → verify a sizes section appears with an "Add size +" button
6. Tap "Add size +" → verify a new row with Label / Width / PSI / Qty inputs appears
7. Add 2 size variants: e.g. "10x12" (stock: 8) and "12x14" (stock: 3)
8. Save → item should appear in inventory list with "2 sizes" badge

**Inventory — Low-stock flag:**
9. Edit the item just created → set threshold to 5
10. Set the "12x14" size stock to 2
11. Save → verify the "Low" badge appears on that item in the list

**Inventory — Edit legacy item:**
12. Edit any existing inventory item (created before Phase 13)
13. Verify it defaults to "Other" unit mode with existing unit text preserved
14. Save without changes → verify nothing breaks

**Billing — Size picker:**
15. Go to Billing tab → find the sized item you added
16. Tap it → verify a size picker sheet appears listing "10x12 (8 in stock)" and "12x14 (3 in stock)"
17. Tap "12x14" → picker closes, cart row shows "YourItemName — 12x14"
18. Tap the same item again → picker appears again; select "10x12" → cart now has 2 rows

**Billing — Submit + stock decrement:**
19. Submit the sale
20. Go to Inventory → Edit the sized item
21. Verify "10x12" stock decreased by the qty sold, "12x14" stock decreased by its qty

**Receipt:**
22. Go to last receipt (confirmation screen or re-navigate via sale ID)
23. Long-press receipt image → Share / Download
24. Verify size labels appear below item names (e.g. "12X14" in muted smaller text)
25. Verify non-sized items (if any in the sale) render without blank lines
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass.
Or describe which step failed (e.g. "step 16 — size picker did not appear").
  </resume-signal>
</task>

</tasks>

<output>
After verification passes, create `.planning/phases/13-inventory-piece-sizes/13-03-SUMMARY.md`
and update STATE.md: Phase 13 → ✅ Complete.
</output>
