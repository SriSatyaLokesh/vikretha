---
phase: 26-reports-advanced-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/reports.js
  - styles/main.css
autonomous: true
requirements: [FR-RPT-01, FR-RPT-02, FR-RPT-03, FR-RPT-04, FR-RPT-08, FR-RPT-09, FR-RPT-10]

must_haves:
  truths:
    - "Filter button shows with an active-count badge when filters are applied"
    - "Clicking a preset date (Today, This week, etc.) sets the date range and triggers reload"
    - "Payment method pills (All/Cash/UPI/Card/Split) are clickable and show active state"
    - "Sort dropdown changes the sort order of the list"
    - "Clear All resets every filter to defaults"
  artifacts:
    - path: "modules/reports.js"
      provides: "New filter state variables + filter panel HTML + preset date handlers + payment pill handlers + sort handler"
    - path: "styles/main.css"
      provides: "Filter panel, preset pills, payment pills, sort select, active badge, clear-all button styles"
  key_links:
    - from: "filter panel toggle button"
      to: "#rpt-filter-panel (collapsible div)"
      via: "click → toggle .rpt-filter-panel--open class"
    - from: "preset date buttons"
      to: "_fromDate / _toDate state"
      via: "_applyPreset(preset) → _loadSales(true)"
    - from: "payment pills"
      to: "_payFilter state"
      via: "click → _applyAllFilters() (client-side only, no reload)"
---

<objective>
Redesign the Reports filter bar into a collapsible filter panel with preset date shortcuts, payment method pills, sort dropdown, active filter count badge, and Clear All.

Purpose: Give users fast access to common queries (Today's cash sales) without typing dates manually.
Output: Expanded filter UI with full state management. No filtering logic yet — that comes in Plan 02.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/phases/26-reports-advanced-filters/

**Key interfaces from reports.js (read before editing):**

Current module-level state (lines 14–37):
```javascript
let _allDocs  = [];
let _filtered = [];
let _lastDoc  = null;
let _loading  = false;
let _fromDate = null;  // ISO 'YYYY-MM-DD'
let _toDate   = null;
let _fromTime = null;  // 'HH:MM'
let _toTime   = null;
let _searchTimer = null;
let _escKeyHandler = null;
let _invCache = null;
```

Current filter bar HTML (inside `render()`, lines ~916–940):
```html
<div class="reports-filter-bar">
  <div class="rpt-filter-group">
    <span class="rpt-filter-label">From</span>
    <div class="rpt-datetime-pair">
      <input type="date" id="rpt-from" ...>
      <input type="time" id="rpt-from-time" ...>
    </div>
  </div>
  <span class="rpt-date-sep">→</span>
  <div class="rpt-filter-group">...To...</div>
  <div class="rpt-filter-btns">
    <button id="rpt-filter-btn">Filter</button>
    <button id="rpt-clear-btn">Clear</button>
  </div>
  <input type="search" id="rpt-search" ...>
</div>
```

Current reset in `render()` (lines ~880–895): resets `_fromDate`, `_toDate`, `_fromTime`, `_toTime`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new filter state variables + reset them in render()</name>
  <files>modules/reports.js</files>
  <action>
Add these module-level state variables after the existing `let _toTime = null;` line:

```javascript
// ── Advanced filter state ────────────────────────────────────────────────────
let _payFilter   = 'all';    // 'all' | 'cash' | 'upi' | 'card' | 'split'
let _sortOrder   = 'newest'; // 'newest' | 'oldest' | 'amount_desc' | 'amount_asc' | 'items_desc' | 'items_asc'
let _amtMin      = null;     // number or null
let _amtMax      = null;     // number or null
let _custFilter  = '';       // phone string or ''
let _filterPanelOpen = false;
```

In the `render()` function where existing state is reset (`_fromDate = null` etc.), also reset the new variables:
```javascript
_payFilter   = 'all';
_sortOrder   = 'newest';
_amtMin      = null;
_amtMax      = null;
_custFilter  = '';
_filterPanelOpen = false;
```
  </action>
  <verify>
    <automated>grep -n "_payFilter\|_sortOrder\|_amtMin\|_filterPanelOpen" modules/reports.js | head -20</automated>
  </verify>
  <done>6 new state variables declared at module level and reset in render()</done>
</task>

<task type="auto">
  <name>Task 2: Replace filter bar HTML with the new filter panel structure</name>
  <files>modules/reports.js</files>
  <action>
Replace the entire `<div class="reports-filter-bar">...</div>` block in the `render()` innerHTML with:

```html
<!-- Filter toolbar row: search + toggle button -->
<div class="rpt-toolbar">
  <input type="search" id="rpt-search" class="rpt-search-input"
         placeholder="Search by Sale ID, name or phone..." aria-label="Search sales">
  <button id="rpt-filter-toggle" class="btn btn-ghost btn-sm rpt-filter-toggle-btn" aria-expanded="false" aria-controls="rpt-filter-panel">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="6" x2="20" y2="6"/><line x1="8" y1="12" x2="16" y2="12"/><line x1="11" y1="18" x2="13" y2="18"/></svg>
    Filters
    <span id="rpt-filter-badge" class="rpt-filter-badge" style="display:none">0</span>
  </button>
  <button id="rpt-export-filtered-btn" class="btn btn-ghost btn-sm" style="display:none" title="Export filtered results to Excel">
    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
    Export
  </button>
</div>

<!-- Collapsible filter panel -->
<div id="rpt-filter-panel" class="rpt-filter-panel" style="display:none" role="region" aria-label="Sales filters">

  <!-- Row 1: Preset date shortcuts -->
  <div class="rpt-filter-row">
    <span class="rpt-filter-label">Date</span>
    <div class="rpt-preset-pills">
      <button class="rpt-preset-pill" data-preset="today">Today</button>
      <button class="rpt-preset-pill" data-preset="yesterday">Yesterday</button>
      <button class="rpt-preset-pill" data-preset="week">This week</button>
      <button class="rpt-preset-pill" data-preset="month">This month</button>
      <button class="rpt-preset-pill" data-preset="last30">Last 30 days</button>
    </div>
  </div>

  <!-- Row 2: Custom date range (from / to) — retains existing inputs -->
  <div class="rpt-filter-row rpt-filter-row--custom-date">
    <div class="rpt-filter-group">
      <span class="rpt-filter-label">From</span>
      <div class="rpt-datetime-pair">
        <input type="date" id="rpt-from" class="rpt-date-input" aria-label="From date">
        <input type="time" id="rpt-from-time" class="rpt-time-input" aria-label="From time">
      </div>
    </div>
    <span class="rpt-date-sep">&#x2192;</span>
    <div class="rpt-filter-group">
      <span class="rpt-filter-label">To</span>
      <div class="rpt-datetime-pair">
        <input type="date" id="rpt-to" class="rpt-date-input" aria-label="To date">
        <input type="time" id="rpt-to-time" class="rpt-time-input" aria-label="To time">
      </div>
    </div>
    <div class="rpt-filter-btns">
      <button id="rpt-filter-btn" class="btn btn-primary btn-sm">Apply</button>
    </div>
  </div>

  <!-- Row 3: Payment method pills -->
  <div class="rpt-filter-row">
    <span class="rpt-filter-label">Payment</span>
    <div class="rpt-pay-pills">
      <button class="rpt-pay-pill rpt-pay-pill--active" data-pay="all">All</button>
      <button class="rpt-pay-pill" data-pay="cash">Cash</button>
      <button class="rpt-pay-pill" data-pay="upi">UPI</button>
      <button class="rpt-pay-pill" data-pay="card">Card</button>
      <button class="rpt-pay-pill" data-pay="split">Split</button>
    </div>
  </div>

  <!-- Row 4: Amount range + Sort -->
  <div class="rpt-filter-row rpt-filter-row--inline">
    <div class="rpt-filter-group">
      <span class="rpt-filter-label">Min &#x20b9;</span>
      <input type="number" id="rpt-amt-min" class="rpt-amt-input" placeholder="0" min="0" aria-label="Minimum amount">
    </div>
    <span class="rpt-date-sep">&#x2013;</span>
    <div class="rpt-filter-group">
      <span class="rpt-filter-label">Max &#x20b9;</span>
      <input type="number" id="rpt-amt-max" class="rpt-amt-input" placeholder="Any" min="0" aria-label="Maximum amount">
    </div>
    <div class="rpt-filter-group rpt-sort-group">
      <span class="rpt-filter-label">Sort by</span>
      <select id="rpt-sort" class="rpt-sort-select" aria-label="Sort order">
        <option value="newest">Newest first</option>
        <option value="oldest">Oldest first</option>
        <option value="amount_desc">Amount &#x2193;</option>
        <option value="amount_asc">Amount &#x2191;</option>
        <option value="items_desc">Items &#x2193;</option>
        <option value="items_asc">Items &#x2191;</option>
      </select>
    </div>
  </div>

  <!-- Row 5: Clear all -->
  <div class="rpt-filter-row rpt-filter-row--actions">
    <button id="rpt-clear-btn" class="btn btn-sm btn-ghost">&#x2715; Clear all filters</button>
  </div>

</div>
```

Also, find the event-handler section that previously wired up `#rpt-filter-btn`, `#rpt-clear-btn`, `#rpt-search` and update it:

1. **`#rpt-filter-toggle` click** — toggle `_filterPanelOpen`, show/hide `#rpt-filter-panel`, update `aria-expanded`, update button text (keep badge).

2. **`.rpt-preset-pill` clicks** — call `_applyPreset(preset)` (define this function):
   ```javascript
   function _applyPreset(preset) {
     const now = new Date();
     const toISO = d => d.toISOString().slice(0, 10);
     let from, to;
     if (preset === 'today') {
       from = to = toISO(now);
     } else if (preset === 'yesterday') {
       const y = new Date(now); y.setDate(now.getDate() - 1);
       from = to = toISO(y);
     } else if (preset === 'week') {
       const start = new Date(now); start.setDate(now.getDate() - now.getDay());
       from = toISO(start); to = toISO(now);
     } else if (preset === 'month') {
       from = toISO(new Date(now.getFullYear(), now.getMonth(), 1)); to = toISO(now);
     } else if (preset === 'last30') {
       const start = new Date(now); start.setDate(now.getDate() - 30);
       from = toISO(start); to = toISO(now);
     }
     _fromDate = from; _toDate = to; _fromTime = null; _toTime = null;
     const fromEl = document.getElementById('rpt-from');
     const toEl   = document.getElementById('rpt-to');
     if (fromEl) fromEl.value = from;
     if (toEl)   toEl.value   = to;
     // Highlight active preset
     document.querySelectorAll('.rpt-preset-pill').forEach(b => b.classList.toggle('rpt-preset-pill--active', b.dataset.preset === preset));
     _loadSales(true);
     _updateFilterBadge();
   }
   ```

3. **`#rpt-filter-btn` (Apply)** — existing logic: read from/to date inputs → set `_fromDate`, `_toDate`, `_fromTime`, `_toTime` → `_loadSales(true)` → `_updateFilterBadge()`.

4. **`#rpt-clear-btn` (Clear all)** — reset ALL filter state: `_fromDate = _toDate = _fromTime = _toTime = null; _payFilter = 'all'; _sortOrder = 'newest'; _amtMin = null; _amtMax = null; _custFilter = '';`. Clear UI inputs, reset payment pills to "All" active, reset sort to "newest". Call `_loadSales(true)` + `_updateFilterBadge()`.

5. **`.rpt-pay-pill` clicks** — set `_payFilter = btn.dataset.pay`, update active class on pills, call `_applyAllFilters()` + `_updateFilterBadge()`. NOTE: `_applyAllFilters()` is defined in Plan 02 — for now, add a stub: `function _applyAllFilters() { _applySearch(); }` (will be replaced in Plan 02).

6. **`#rpt-sort` change** — set `_sortOrder = select.value`, call `_applyAllFilters()`.

7. **`#rpt-amt-min` / `#rpt-amt-max` change** — set `_amtMin`/`_amtMax`, debounce 400ms, call `_applyAllFilters()` + `_updateFilterBadge()`.

8. **`_updateFilterBadge()`** (define this helper):
   ```javascript
   function _updateFilterBadge() {
     let count = 0;
     if (_fromDate || _toDate)  count++;
     if (_payFilter !== 'all')  count++;
     if (_amtMin != null || _amtMax != null) count++;
     if (_sortOrder !== 'newest') count++;
     const badge = document.getElementById('rpt-filter-badge');
     if (badge) {
       badge.textContent = count;
       badge.style.display = count > 0 ? 'inline-flex' : 'none';
     }
     // Show export button when any filter active
     const exportBtn = document.getElementById('rpt-export-filtered-btn');
     if (exportBtn) exportBtn.style.display = count > 0 ? '' : 'none';
   }
   ```
  </action>
  <verify>
    <automated>grep -n "rpt-filter-toggle\|rpt-preset-pill\|rpt-pay-pill\|rpt-sort\|_applyPreset\|_updateFilterBadge" modules/reports.js | head -30</automated>
  </verify>
  <done>New filter panel HTML in place; 8 event handlers wired; _applyPreset, _updateFilterBadge helper functions defined; _applyAllFilters stub present</done>
</task>

<task type="auto">
  <name>Task 3: Add CSS for the filter panel, preset pills, payment pills, sort select, and badge</name>
  <files>styles/main.css</files>
  <action>
After the existing `.rpt-filter-btns` rule (around line 724), add:

```css
/* ── Reports Advanced Filters ── */
.rpt-toolbar {
  display: flex; align-items: center; gap: 8px; flex-wrap: wrap;
}
.rpt-toolbar .rpt-search-input { flex: 1; min-width: 160px; }

.rpt-filter-toggle-btn { display: flex; align-items: center; gap: 6px; flex-shrink: 0; position: relative; }
.rpt-filter-badge {
  display: inline-flex; align-items: center; justify-content: center;
  min-width: 18px; height: 18px; padding: 0 5px;
  border-radius: var(--radius-full); background: var(--primary); color: #fff;
  font-size: 0.7rem; font-weight: 700; line-height: 1;
}

.rpt-filter-panel {
  display: flex; flex-direction: column; gap: 10px;
  background: var(--bg-surface); border: 1px solid var(--border);
  border-radius: var(--radius-md); padding: 14px 16px;
}
.rpt-filter-row {
  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
}
.rpt-filter-row--custom-date { align-items: flex-end; }
.rpt-filter-row--inline { align-items: flex-end; gap: 8px; }
.rpt-filter-row--actions { justify-content: flex-end; padding-top: 2px; border-top: 1px solid var(--border); }

.rpt-preset-pills { display: flex; gap: 6px; flex-wrap: wrap; }
.rpt-preset-pill {
  padding: 4px 12px; border-radius: var(--radius-full); font-size: 0.8rem; font-weight: 500;
  border: 1.5px solid var(--border); background: var(--bg-app); color: var(--text-secondary);
  cursor: pointer; transition: background 0.15s, border-color 0.15s, color 0.15s;
}
.rpt-preset-pill:hover { border-color: var(--primary); color: var(--primary); }
.rpt-preset-pill--active { background: var(--primary-light); border-color: var(--primary); color: var(--primary); font-weight: 600; }

.rpt-pay-pills { display: flex; gap: 6px; flex-wrap: wrap; }
.rpt-pay-pill {
  padding: 4px 14px; border-radius: var(--radius-full); font-size: 0.8rem; font-weight: 500;
  border: 1.5px solid var(--border); background: var(--bg-app); color: var(--text-secondary);
  cursor: pointer; transition: background 0.15s, border-color 0.15s, color 0.15s;
}
.rpt-pay-pill:hover { border-color: var(--primary); color: var(--primary); }
.rpt-pay-pill--active { background: var(--primary); border-color: var(--primary); color: #fff; font-weight: 600; }

.rpt-amt-input {
  width: 90px; padding: 6px 10px; font-size: 0.85rem;
  border: 1.5px solid var(--border); border-radius: var(--radius-sm);
  background: var(--bg-app); color: var(--text-primary);
}
.rpt-amt-input:focus { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-ring); }

.rpt-sort-group { flex: 1; min-width: 140px; }
.rpt-sort-select {
  width: 100%; padding: 6px 10px; font-size: 0.85rem;
  border: 1.5px solid var(--border); border-radius: var(--radius-sm);
  background: var(--bg-app); color: var(--text-primary); cursor: pointer;
}
.rpt-sort-select:focus { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-ring); }

@media (max-width: 767px) {
  .rpt-toolbar { flex-wrap: wrap; }
  .rpt-filter-row--inline { flex-wrap: wrap; }
  .rpt-amt-input { width: 75px; }
  .rpt-sort-group { width: 100%; min-width: unset; }
  .rpt-sort-select { width: 100%; }
}
```

Also update `.rpt-stats-bar` to add wrapping for wider content:
```css
.rpt-stats-bar { display:flex; align-items:center; gap:8px; flex-wrap:wrap; padding:4px 0; }
```
  </action>
  <verify>
    <automated>grep -n "rpt-filter-panel\|rpt-preset-pill\|rpt-pay-pill\|rpt-filter-badge\|rpt-amt-input\|rpt-sort-select" styles/main.css | head -20</automated>
  </verify>
  <done>All CSS classes present; responsive mobile rules added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → filter inputs | Date, amount, phone inputs are read client-side only; no server writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | amount min/max inputs | accept | Values used for client-side filtering only; no server call; NaN/negative handled by `parseFloat` check in Plan 02 |
| T-26-02 | Spoofing | preset date manipulation | accept | Preset dates computed from `new Date()` client-side; no auth implication |
</threat_model>

<verification>
1. Filter toggle button appears in reports Sales tab
2. Clicking toggle opens/closes the filter panel
3. Clicking a preset date pill updates the date inputs and reloads sales
4. Payment pills highlight on click
5. Sort dropdown renders correctly
6. Clear all button resets UI to defaults
7. Filter badge shows count of active filters
8. No JS errors in console
</verification>

<success_criteria>
Filter panel UI is interactive and state-driven. All 8 filter controls render and respond to clicks. Badge count is accurate. No filter logic yet — that is Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/26-reports-advanced-filters/26-01-SUMMARY.md`
</output>
