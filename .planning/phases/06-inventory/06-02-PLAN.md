---
phase: 06-inventory
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: false
requirements:
  - FR-05.1
  - FR-05.2
  - FR-05.3
  - FR-05.4
  - FR-05.5

must_haves:
  truths:
    - "All Phase 6 UAT criteria verified by the user in the browser"
    - "Inventory CRUD confirmed working against real Firestore data"
---

<objective>
Human verification of the complete Phase 6 inventory management implementation.

Purpose: Confirm all list, search, sort, add, edit, and delete functionality works
correctly in the browser against real Firestore data before marking Phase 6 complete.

Output: Phase 6 UAT sign-off and 06-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-inventory/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 6 inventory management module (modules/inventory.js):
    - Live-updating inventory list via Firestore onSnapshot
    - Low-stock visual badges (red "Low" pill on items below threshold)
    - Client-side search (filter by name) and sort toggle (Name A–Z / Low Stock first)
    - FAB "+ Add Item" → bottom-sheet form (name, unit, price, qty, threshold)
    - Tap item row → Edit bottom-sheet (pre-filled, readonly name, updateDoc)
    - Delete with inline confirmation (deleteDoc)
    - Fully offline-capable (Firestore SDK queues all writes)
  </what-built>
  <how-to-verify>
    1. Navigate to **#/inventory** in the app.
       ✓ List appears immediately (from Firestore cache) — no loading spinner hang.

    2. **Low-stock flag**: Find an item with low stock (or temporarily set threshold higher
       than stock in Firestore directly to test). Confirm the red "Low" badge appears.

    3. **Search**: Type part of an item name (e.g., "rice") in the search box.
       ✓ List narrows to matching items instantly.

    4. **Sort**: Toggle between "Name" and "Low Stock" sort buttons.
       ✓ "Name" shows items alphabetically; "Low Stock" puts flagged items first.

    5. **Add item**:
       - Tap the "+ Add Item" FAB (bottom-right).
       - Fill in: Name="Test Item", Unit="pcs", Price=10, Qty=3, Threshold=5.
       - Tap Save.
       ✓ Modal closes, "Test Item" appears in the list with a "Low" badge (stock 3 < threshold 5).

    6. **Edit item**:
       - Tap "Test Item" row.
       - Change Qty to 20, tap Save.
       ✓ Modal closes, "Test Item" now shows stock 20, "Low" badge disappears.

    7. **Delete item**:
       - Tap "Test Item" row → Edit modal opens.
       - Tap Delete → confirm row appears ("Are you sure?").
       - Tap "Yes, delete".
       ✓ Modal closes, "Test Item" removed from list.

    8. **Offline (optional)**: Set browser to offline (DevTools → Network → Offline).
       - Add a new item.
       - ✓ No error shown; item appears in UI optimistically (Firestore queued).
       - Go back online — item persists in Firestore.

    9. **Billing cross-check**: Navigate to #/billing, search for an existing inventory item.
       ✓ It still appears in billing product grid (inventory.js onSnapshot does not interfere
       with billing.js's own listener).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 6 complete, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All Phase 6 UAT items from ROADMAP.md confirmed:
- [ ] Inventory loads instantly from Firestore cache
- [ ] Low-stock items visually flagged
- [ ] Add new item → appears in list
- [ ] Edit item → changes reflected
- [ ] Stock decrements correctly after billing (cross-check)
- [ ] Works fully offline (reads from cache, writes queued)
</verification>

<success_criteria>
User types "approved" — Phase 6 complete.
STATE.md updated to Phase 6 complete, Phase 7 next.
</success_criteria>

<output>
After completion, create `.planning/phases/06-inventory/06-02-SUMMARY.md`

Update STATE.md:
- Phase: 6 (Inventory Management — ✅ Complete)
- Next action: Plan Phase 7 (/gsd-plan-phase 7)
- Phase 6 row: ✅ Complete
</output>
