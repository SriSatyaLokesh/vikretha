---
phase: 08-settings
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: []
autonomous: false
requirements: [FR-01.7, FR-01.9]

must_haves:
  truths:
    - "Settings screen accessible from bottom nav"
    - "Authorized email list displayed correctly"
    - "Add email grants access to new staff member"
    - "Remove email revokes access"
    - "Cannot remove own email"
    - "Sign out clears session"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Staff Management & Settings feature.

Purpose: Confirm all staff access management flows work end-to-end against a live Firestore instance, including edge cases (invalid email, self-removal attempt, offline).

Output: Human approval → Phase 8 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-settings/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Staff Management & Settings screen:
    - Staff email list loaded from /shops/{SHOP_ID}/config.authorized_emails
    - Add Email form with validation (writes arrayUnion to Firestore)
    - Remove Email with confirmation dialog (writes arrayRemove, disabled for own email)
    - Sign Out button
    - CSS Section 19 styles
  </what-built>
  <how-to-verify>
    1. Open the app: http://localhost:8080 (or GitHub Pages URL)
    2. Sign in and navigate to **Settings** (bottom nav, last icon)
    3. **Signed in as card:** Your email should appear under "Signed in as"
    4. **Staff list loads:** "Staff Access" section shows a list of authorized emails with Remove buttons
    5. **Own email disabled:** Your own email should have a greyed-out/disabled Remove button (cannot self-lock)
    6. **Add valid email:**
       - Type a new email (e.g., `test@example.com`) → click Add
       - Loading state shown, then list refreshes with new email
       - Open Firebase Console → Firestore → shops/{shop_id}/config → verify `authorized_emails` array updated
    7. **Add invalid email:**
       - Type `notanemail` → click Add → error message "Please enter a valid email address." shown, no Firestore write
    8. **Remove email:**
       - Click Remove on an email that is NOT yours
       - Confirm dialog appears → click OK
       - Email removed from list, Firestore updated
    9. **Cancel remove:**
       - Click Remove → click Cancel → email stays in list
    10. **Sign out:**
        - Click Sign Out → app returns to login screen
        - Re-sign-in works normally
  </how-to-verify>
  <resume-signal>Type "approved" if all staff management flows work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verified:
- [ ] Staff list renders from Firestore
- [ ] Add email writes arrayUnion correctly
- [ ] Remove email writes arrayRemove correctly
- [ ] Own email Remove is disabled
- [ ] Invalid email shows error, not submitted
- [ ] Sign out works
</verification>

<success_criteria>
- All staff management flows verified by human
- No console errors during normal operation
- Firestore writes confirmed in Firebase Console
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/08-settings/08-02-SUMMARY.md`
</output>
