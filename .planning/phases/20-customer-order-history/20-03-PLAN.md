---
phase: 20-customer-order-history
plan: "03"
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified: []
autonomous: false
requirements: [FR-11.1, FR-11.2, FR-11.3, FR-11.4, FR-11.5]

must_haves:
  truths:
    - "All 6 UAT criteria from ROADMAP Phase 20 verified by shop owner"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 20 — Customer Order History.

Purpose: Shop owner confirms customer lookup, aggregate stats, bill detail, receipt navigation, and pagination all work correctly in the live app.
Output: Phase 20 approved and complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 20: Customer Order History

    - Reports screen now has "Sales" and "Customers" tabs
    - Customers tab: phone number search → customer card (name, total spend, bill count, last sale date)
    - Paginated bill list below the card; tapping any bill opens the full sale detail panel
    - Receipt screen: "👤 Customer history →" link (only when sale has customer phone) → navigates to that customer's panel in Reports
  </what-built>
  <how-to-verify>
    1. Open the app → navigate to **Reports**
    2. Confirm two tabs are visible: **Sales** (active by default) and **Customers**
    3. Confirm the Sales tab still works as before (list loads, filters work)
    4. Click **Customers** tab → phone input appears; filter bar and stats bar are hidden
    5. Type a phone number for a customer with past bills → customer card appears showing:
       - Customer name and phone
       - Total Spend (sum of all their bills)
       - Bills count
       - Last Sale date
    6. Verify stats are correct (cross-check with 1–2 known bills in Sales tab)
    7. Tap a bill row in the list → **sale detail panel opens** with full item breakdown
    8. Confirm the "🧾 View / Resend Receipt" button in the panel works
    9. Test **pagination**: if the customer has more than 25 bills, confirm "Load more" appears and loads older bills
    10. Type a phone that has **no customer record** → "No customer found" message shown
    11. Open a **receipt** for a sale that has a customer phone → "👤 Customer history →" link visible below the WhatsApp button
    12. Tap that link → navigates to Reports Customers tab with that customer's panel already open
    13. Open a **receipt** for a sale **without** a customer phone → no customer history link visible
    14. Test on **mobile** (≤ 430 px screen) — customer card and bill list layout looks correct, no overflow
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 20, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
UAT criteria from ROADMAP Phase 20:
- [ ] Reports screen shows "Sales" and "Customers" tabs
- [ ] Typing a phone number in Customers tab shows matching customer (or "No customer found")
- [ ] Customer panel shows correct name, total spend, bill count, last sale date
- [ ] Tapping a past bill opens the full sale detail panel
- [ ] "Customer history →" link on receipt screen navigates to that customer's panel
- [ ] Pagination works: "Load more" shows older bills beyond 25
</verification>

<success_criteria>
All 6 UAT criteria from ROADMAP Phase 20 verified by shop owner.
</success_criteria>

<output>
After completion, create `.planning/phases/20-customer-order-history/20-03-SUMMARY.md`
</output>
